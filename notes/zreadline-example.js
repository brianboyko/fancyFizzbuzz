Discovered a bug where it was incorrectly shunting to process all integers as big integers;
this was inefficient but did not affect functionality. 

Fixing that bug caused me to discover a bug (in production) where a variable was misnamed. Fixed. 